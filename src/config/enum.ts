export const enum Direction {
  top = 0,
  right,
  down,
  left,
}

export const enum EnemyType {
  normal = 1,
  enhance,
  fast,
  armor,
}

export const enum Control {
  up,
  right,
  down,
  left,
  shoot,
}
